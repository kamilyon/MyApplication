import android.content.Context;
import android.support.annotation.Nullable;
import android.support.v7.widget.RecyclerView;
import android.util.AttributeSet;

/**
 * Created by Zealya on 3/8/2018.
 */

public class MovieDataAdapter extends RecyclerView {
    public MovieDataAdapter(Context context) {
        super(context);
    }

    public MovieDataAdapter(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public MovieDataAdapter(Context context, @Nullable AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
    }
}
